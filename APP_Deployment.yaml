apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
  labels:
    app: app-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app-server
  template:
    metadata:
      labels:
        app: app-server
    spec:
      containers:
      - name: app
        image: tegexplore/pda-2022:app_branch-7eab9f8
        ports:
        - containerPort: 8000
      imagePullSecrets:
      - name: dockercred
